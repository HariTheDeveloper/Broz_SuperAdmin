import 'package:flutter/material.dart';
import 'package:push_notification/Data/user_logs_data.dart';

class CustomerCell extends StatelessWidget {
  final CustomerLogs offersJson;

  const CustomerCell({Key key, @required this.offersJson})
      : assert(offersJson != null),
        super(key: key);

  @override
  Widget build(BuildContext context) {
    ;
  }
}
